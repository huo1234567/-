```text
# AI 协作反思

## 一、使用的 AI 工具
使用了deepseek和豆包,豆包主要负责基础代码编写部分,deepseek主要负责一些豆包无法解决的问题或者一些报错问题

## 二、AI 节省时间最多的环节
在本次文本搜索系统的开发中，AI 极大缩短了我的开发周期，其中节省时间最多的环节集中在底层数据结构、复杂算法实现与界面框架搭建这三部分。首先是字典树（Trie）的实现，Trie 节点结构、前缀搜索、DFS
递归补全逻辑如果手动编写，需要反复调试指针与递归边界，AI 可以直接给出可运行的标准模板，让我几分钟内完成原本可能需要一小时的编码工作。其次是分块倒排索引的分治逻辑，包括文档切块、子索引构建、
合并去重排序，逻辑繁琐且容易出错，AI 能一次性给出完整流程，避免了大量调试时间。再者是 Top-K 堆排序、编辑距离拼写纠错 这类算法，手写容易出现边界错误，AI 可直接提供高效稳定的实现代码。最后是
Tkinter 图形界面，从窗口布局、组件排版到滚动日志、按钮绑定，AI 能快速生成完整框架，省去了查阅文档和调试界面样式的大量时间。整体来看，AI 将原本需要数天的编码与调试工作量压缩到几小时，
尤其在算法、结构、界面这类重复性高、逻辑严谨的环节，效率提升最为明显。

## 三、AI 把我带偏的环节
在开发过程中，AI 也在一些环节将我带偏，导致走了不必要的弯路。最明显的是前期过度追求复杂实现，例如在做前缀补全功能时，AI 给出了过于完善的扩展版本，包含大小写兼容、多字符串匹配、前缀排序等额外功能，
而项目实际只需要基础前缀匹配，我花费大量时间理解多余代码，反而忽略了核心逻辑。其次是 撤销功能的实现方式，AI 最初建议使用字符串拼接存储操作类型，导致频繁出现分割报错、数据异常，我反复调试却始终不稳定，
后来才发现是数据结构设计不合理，浪费了大量时间。此外，AI 在界面优化上不断提供新功能建议，如实时提示、一键替换、预览文档等，让我偏离了 “先完成核心功能” 的目标，不断添加无关功能，导致主线进度滞后。还有
性能测试部分，AI 最初给出的方案过于复杂，包含随机文本生成、多文件夹批量测试，偏离了 “统计建索引与 100 次查询耗时” 的简单需求，使我在无关逻辑上浪费大量精力。这些偏差让我明白，AI 提供的方案往往追求全面，
而非最适合项目的最简实现，容易让人陷入过度设计。

## 四、独立完成与不能独立完成的部分
回顾整个项目，我能清晰区分独立可完成部分与必须依靠 AI 辅助才能完成的部分。我可以独立完成的内容包括：文档读取与文本切词、基础倒排索引的构建、简单查询功能、历史记录队列、基础图形界面布局与日志输出。
这些部分逻辑直观、步骤清晰，属于流程化编程任务，我可以依靠课程知识独立实现，也能自行调试简单错误。而无法独立完成的部分主要集中在复杂数据结构与算法：字典树（Trie）的递归前缀补全、分块索引的分治合并逻辑、
Top-K 小顶堆优化、编辑距离动态规划拼写纠错，以及稳定的撤销栈功能设计。这些内容算法性强、边界条件多、手写易出错，我很难一次性写出无 Bug 的可运行代码。尤其是撤销功能需要存储查询类型与参数，不借助 AI 很难想
到用字典结构保存操作信息；Trie 树的 DFS 递归与分块索引合并策略也超出了我当前的独立实现能力。整体而言，流程化、结构化的功能我可以独立完成，但涉及复杂算法、递归、高级数据结构与性能优化的部分，必须依靠 AI
提供示例与修正，才能顺利完成项目。

## 五、AI 翻车案例

### 5.1 我向 AI 提出的问题
### 5.2 AI 给我的代码
### 5.3 我发现的问题
### 5.4 我的修正过程
### 5.5 我从中学到了什么
```
