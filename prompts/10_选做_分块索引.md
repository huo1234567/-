```
A1:将功能二优化为分块索引
**要求**：使用分治思想，将所有文档划分为不少于 3 个子集，分别建立子索引，再将子索引合并为全局索引。
**验收标准**：源码中明确划分子集与合并的逻辑(注释标注)；
R1:已完成：功能 2 优化为 分块索引（分治思想）
我已经严格按照要求把原来的全局索引，优化为：
分治思想：将文档分成 3 个以上块
分别建立子索引
合并子索引 → 全局索引
代码完整注释，清晰区分分块、建子索引、合并逻辑
所有原有功能 100% 保留（Trie、TopK、纠错、撤销、历史…）
import os
import heapq

# ==================== 功能1：文档读取与切词 ====================
def tokenize_text(text: str) -> list[str]:
    tokens = []
    current_buffer = []
    text = text.lower()

    for char in text:
        is_chinese = '\u4e00' <= char <= '\u9fff'
        is_letter = char.isalpha()
        is_digit = char.isdigit()

        if is_chinese:
            if current_buffer:
                tokens.append(''.join(current_buffer))
                current_buffer.clear()
            tokens.append(char)
        elif is_letter or is_digit:
            current_buffer.append(char)
        else:
            if current_buffer:
                tokens.append(''.join(current_buffer))
                current_buffer.clear()

    if current_buffer:
        tokens.append(''.join(current_buffer))
    return tokens

def load_and_tokenize(folder_path: str) -> dict[int, list[str]]:
    txt_files = [f for f in os.listdir(folder_path) if f.endswith('.txt')]
    txt_files.sort()
    doc_tokens = {}
    for idx, filename in enumerate(txt_files):
        file_path = os.path.join(folder_path, filename)
        try:
            with open(file_path, 'r', encoding='utf-8') as f:
                content = f.read()
            tokens = tokenize_text(content)
            doc_tokens[idx] = tokens
        except Exception as e:
            print(f"读取文件 {filename} 失败：{str(e)}")
            doc_tokens[idx] = []
    return doc_tokens

# ==================== 功能2：分块索引（分治思想）优化版 ====================
# ==================== 【分治：分块 + 建子索引 + 合并】 ====================
def split_into_blocks(doc_dict: dict, block_num=3):
    """
    分治 - 分：将所有文档分成 >=3 个块
    输入：全体文档 {编号: 词项列表}
    输出：[块1, 块2, 块3]
    """
    docs = list(doc_dict.items())
    total = len(docs)
    block_size = (total + block_num - 1) // block_num  # 向上取整
    blocks = []
    for i in range(block_num):
        start = i * block_size
        end = start + block_size
        block = dict(docs[start:end])
        if block:
            blocks.append(block)
    return blocks

def build_sub_index(block: dict):
    """
    分治 - 治：为单个块建立子索引
    子索引格式：{词: [文档编号列表]}
    """
    sub_idx = {}
    for doc_id, tokens in block.items():
        unique_terms = set(tokens)
        for term in unique_terms:
            if term not in sub_idx:
                sub_idx[term] = []
            sub_idx[term].append(doc_id)
            sub_idx[term].sort()
    return sub_idx

def merge_sub_indexes(sub_indexes: list):
    """
    分治 - 合：合并所有子索引 → 全局索引
    相同词项的文档编号合并 + 去重 + 排序
    """
    global_idx = {}
    for sub in sub_indexes:
        for term, doc_ids in sub.items():
            if term not in global_idx:
                global_idx[term] = []
            global_idx[term].extend(doc_ids)
            # 去重 + 排序
            global_idx[term] = sorted(list(set(global_idx[term])))
    return global_idx

def build_block_index(doc_dict: dict, block_num=3):
    """
    分块索引总入口（分 + 治 + 合）
    """
    # 1. 分：切块
    blocks = split_into_blocks(doc_dict, block_num)
    # 2. 治：为每个块建子索引
    sub_indexes = [build_sub_index(block) for block in blocks]
    # 3. 合：合并成全局索引
    global_index = merge_sub_indexes(sub_indexes)
    return global_index

def search_word(word: str, inverted_index: dict[str, list[int]]) -> list[int]:
    if not word:
        return []
    word = word.lower()
    return inverted_index.get(word, [])

# ==================== 功能3：自定义撤销栈 ====================
class UndoStack:
    def __init__(self):
        self.stack = []
        self.top = -1

    def push(self, item):
        self.top += 1
        self.stack.append(item)

    def pop(self):
        if self.is_empty():
            return None
        item = self.stack[self.top]
        del self.stack[self.top]
        self.top -= 1
        return item

    def peek(self):
        if self.is_empty():
            return None
        return self.stack[self.top]

    def is_empty(self):
        return self.top < 0

# ==================== 功能3：自定义历史队列 ====================
class HistoryQueue:
    def __init__(self, capacity=10):
        self.queue = []
        self.capacity = capacity

    def enqueue(self, item):
        if len(self.queue) >= self.capacity:
            self.dequeue()
        self.queue.append(item)

    def dequeue(self):
        if not self.queue:
            return None
        return self.queue.pop(0)

    def remove_last(self):
        if self.queue:
            self.queue.pop()

    def recent(self):
        return list(self.queue)

# ==================== 功能4：Top-K 相关性排序（A+B方案） ====================
def top_k_with_score(word: str, k: int, doc_tokens: dict[int, list[str]]):
    word = word.lower()
    scores = []
    for doc_id, tokens in doc_tokens.items():
        cnt = tokens.count(word)
        if cnt > 0:
            scores.append((-cnt, doc_id, cnt))
    scores.sort()
    top_list = [(sid, cnt) for (_, sid, cnt) in scores[:k]]
    return top_list

def top_k_heap_with_score(word: str, k: int, doc_tokens: dict[int, list[str]]):
    word = word.lower()
    heap = []
    for doc_id, tokens in doc_tokens.items():
        cnt = tokens.count(word)
        if cnt > 0:
            if len(heap) < k:
                heapq.heappush(heap, (cnt, doc_id))
            else:
                if cnt > heap[0][0]:
                    heapq.heappop(heap)
                    heapq.heappush(heap, (cnt, doc_id))
    heap_sorted = sorted(heap, key=lambda x: (-x[0], x[1]))
    return [(sid, cnt) for cnt, sid in heap_sorted]

# ==================== 功能5：拼写纠错（DP编辑距离） ====================
def edit_distance(s1: str, s2: str) -> int:
    m, n = len(s1), len(s2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(m + 1):
        dp[i][0] = i
    for j in range(n + 1):
        dp[0][j] = j
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if s1[i-1] == s2[j-1]:
                dp[i][j] = dp[i-1][j-1]
            else:
                dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])
    return dp[m][n]

def spell_correct(query: str, vocab: set) -> str | None:
    query = query.lower()
    if not query.isalpha() or any('\u4e00' <= c <= '\u9fff' for c in query):
        return None
    min_dist = float('inf')
    best_word = None
    for word in vocab:
        if not word.isalpha():
            continue
        dist = edit_distance(query, word)
        if dist < min_dist:
            min_dist = dist
            best_word = word
    return best_word

# ==================== 功能6：字典树（Trie）前缀补全 ====================
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()

    def insert(self, word):
        node = self.root
        for c in word:
            if c not in node.children:
                node.children[c] = TrieNode()
            node = node.children[c]
        node.is_end = True

    def starts_with(self, prefix):
        node = self.root
        for c in prefix:
            if c not in node.children:
                return []
            node = node.children[c]
        res = []
        self.dfs(node, prefix, res)
        return res

    def dfs(self, node, path, res):
        if node.is_end:
            res.append(path)
        for c, child in node.children.items():
            self.dfs(child, path + c, res)

def build_trie(vocab: set) -> Trie:
    trie = Trie()
    for word in vocab:
        trie.insert(word)
    return trie

# ==================== 主程序 ====================
if __name__ == "__main__":
    print("==================================================")
    print("        中文文本搜索系统 全功能最终版（分块索引）")
    print("==================================================")

    try:
        doc_dict = load_and_tokenize("testdata/")
        
        # ==================== 调用分块索引构建 ====================
        # 分3块建立索引（满足>=3要求）
        inverted_index = build_block_index(doc_dict, block_num=3)
        
        vocab = set(inverted_index.keys())
        trie = build_trie(vocab)

        undo_stack = UndoStack()
        history_queue = HistoryQueue()

        print("========== 系统功能菜单 ==========")
        print("1. 普通查询：直接输入查询词")
        print("2. Top-K查询：topk:词:k:A / topk:词:k:B")
        print("3. 撤销上一次：undo")
        print("4. 查看历史：history")
        print("5. 显示全部切词：show")
        print("6. 前缀补全：prefix:字符")
        print("7. 退出：exit")
        print("================================\n")

        while True:
            user_input = input("请输入指令/查询词：").strip()

            if user_input.lower() == "exit":
                print("系统已退出")
                break

            elif user_input.lower() == "undo":
                canceled_word = undo_stack.pop()
                if canceled_word is None:
                    print("→ 无操作可撤销\n")
                    continue
                history_queue.remove_last()
                canceled_res = search_word(canceled_word, inverted_index)
                print(f"→ 已撤销上一次查询：[{canceled_word}] → {canceled_res}\n")
                continue

            elif user_input.lower() == "history":
                print(f"最近10次查询记录（早→晚）：{history_queue.recent()}\n")
                continue

            elif user_input.lower() == "show":
                print("\n========== 全部文档切词结果 ==========")
                for doc_id, tokens in doc_dict.items():
                    print(f"文档{doc_id} 词项列表：{tokens}")
                    print(f"文档{doc_id} 词项总数：{len(tokens)}\n")
                continue

            elif user_input.startswith("prefix:"):
                prefix = user_input.split(":", 1)[1].strip().lower()
                matches = trie.starts_with(prefix)
                print(f"→ 前缀【{prefix}】匹配结果：{matches}\n")
                continue

            elif user_input.startswith("topk:"):
                parts = user_input.split(":")
                if len(parts) != 4:
                    print("→ 格式错误！示例：topk:算:3:A\n")
                    continue
                try:
                    word = parts[1]
                    k = int(parts[2])
                    mode = parts[3].upper()
                    if mode == "A":
                        res = top_k_with_score(word, k, doc_dict)
                        print("→ 方案A Top-K 结果：")
                    elif mode == "B":
                        res = top_k_heap_with_score(word, k, doc_dict)
                        print("→ 方案B Top-K 结果：")
                    else:
                        print("→ 仅支持 A / B 两种方案\n")
                        continue
                    for doc_id, score in res:
                        print(f"   文档 {doc_id} (相关度: {score})")
                    print()
                    undo_stack.push(user_input)
                    history_queue.enqueue(user_input)
                except:
                    print("→ 输入格式错误！\n")
                continue

            if user_input:
                res = search_word(user_input, inverted_index)
                if res:
                    print(f"→ 搜索结果：[{user_input}] → {res}\n")
                    undo_stack.push(user_input)
                    history_queue.enqueue(user_input)
                else:
                    suggest = spell_correct(user_input, vocab)
                    if suggest:
                        print(f"→ 未找到该词，是否要查询：{suggest} ？\n")
                    else:
                        print(f"→ 未找到 [{user_input}]，无匹配内容\n")

    except FileNotFoundError:
        print("错误：未找到 testdata 文件夹，请检查目录！")
```
